- name: install the nginx
  hosts: frontend
  connection: local
  become: yes
  task:
  - name: install nginx
    ansible.builtin.package:
     name: nginx
     state: present

  - name: run the nginx
    ansible.builtin.service:
     name: nginx
     state: started


