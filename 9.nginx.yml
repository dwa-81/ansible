- name: install the nginx and run
  hosts: frontend
  task:
   - name: install nginx
     ansible.builtin.package:
       name: nginx
       state: present
 
   - name: run the nginx
     ansible.builtin.service:
       name: nginx
       state: started


